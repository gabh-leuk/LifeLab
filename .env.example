# 根目录 .env —— 只给 docker compose 用（起开发数据库 PostgreSQL）。
# 复制为 .env 并填一个强口令：cp .env.example .env
# 该文件被 .gitignore 覆盖，绝不进仓库。
#
# 注意：backend/.env 的 DATABASE_URL 里的口令必须与这里相同。
LIFELAB_DB_PASSWORD=change-me-strong-password
